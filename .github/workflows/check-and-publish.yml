name: Check and Publish

env:
  # Unified cache key used across all jobs and both caching mechanisms (rust-cache + actions/cache)
  RUST_UNIFIED_CACHE_KEY: v1-${{ runner.os }}-${{ hashFiles('**/Cargo.lock') }}

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  deno-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x
          cache: true
      - run: deno task ok

  rust-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/cache@v4
        with:
          path: target
          key: ${{ env.RUST_UNIFIED_CACHE_KEY }}
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: ${{ env.RUST_UNIFIED_CACHE_KEY }}
          cache-targets: "false"
      - run: cargo fmt --check
      - run: cargo clippy -- -D warnings

  publish:
    runs-on: ubuntu-latest
    needs: [rust-check, deno-check]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x
          cache: true
      - uses: actions/cache@v4
        with:
          path: target
          key: ${{ env.RUST_UNIFIED_CACHE_KEY }}
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: ${{ env.RUST_UNIFIED_CACHE_KEY }}
          cache-targets: "false"
      - run: deno task build
      - run: deno publish
