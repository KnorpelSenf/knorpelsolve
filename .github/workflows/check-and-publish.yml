name: Check and Publish

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  deno-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x
          cache: true
      - run: deno task ok

  rust-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: rust-v1-${{ runner.os }}-${{ hashFiles('**/Cargo.lock') }}
          cache-targets: "false"
      - uses: actions/cache@v4
        with:
          key: target-v1-${{ runner.os }}-${{ hashFiles('**/Cargo.lock') }}
          path: target
      - run: cargo fmt --check
      - run: cargo clippy -- -D warnings

  publish:
    runs-on: ubuntu-latest
    needs: [rust-check, deno-check]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x
          cache: true
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: rust-v1-${{ runner.os }}-${{ hashFiles('**/Cargo.lock') }}
          cache-targets: "false"
      - uses: actions/cache@v4
        with:
          key: target-v1-${{ runner.os }}-${{ hashFiles('**/Cargo.lock') }}
          path: target
      - run: deno task build
      - run: deno publish
